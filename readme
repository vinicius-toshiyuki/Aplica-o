sudo apt install python3-tk
sudo apt install python3-psycopg2
sudo apt install postgresql
sudo -u postgres psql
\password
root
root
create database corretor;
create table tabela (nome varchar(200), idade integer);
\q
python3 front.py
